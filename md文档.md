# Markdown 快速帮助

## 导出 PDF

选择“文件 → 导出 → PDF 文档…”，指定保存位置即可导出。未保存的编辑内容也会包含在 PDF 中，原 Markdown 文件保持不变。PDF 使用 A4 浅色页面、页码和可选择文字，长表格会分页并重复表头；图片、公式和 Mermaid 图表需要成功加载后才会完成导出。可在进度窗口取消，失败或取消不会破坏已有 PDF。目前支持最多 64 MB 的源文档，Word 等格式将在后续增加。

Emoji 短码在渲染后可按一个字符整体删除。新输入的 `---` 在回车后成为分割线；引用中的任务项也支持自动补框和续行。悬停脚注显示解释内容，点击跳到解释行；图片在正文及列表、引用、表格内都可点击预览，再次点击或 Esc 关闭。

同一分栏内可左右拖动文档标签排序；拖到其他分栏顶部可移动文档。矩形选区跨过模块时保持原有排版，分割线的光标只停在两端，删除按整条处理。

将文档标签拖到另一个分栏的标签栏即可移动；空分栏可放到顶部。右上角资源管理器按钮左边的主题按钮用于快速切换深色和浅色主题。

## 脚注、Emoji 和提示框

脚注示例[^help-note]。点击上标定位定义，点击定义后的箭头返回。

[^help-note]: 注释支持**强调**与`行内代码`，显示在源码中定义的位置。

Emoji 短码示例：:smile: :rocket: :octocat: :shipit: :copilot: :homes:。未知短码保留原文。

> [!NOTE]
> 提示框支持 NOTE、TIP、IMPORTANT、WARNING、CAUTION 五种类型。首行写 `> [!NOTE]`，后续正文继续使用引用前缀。

> 帮助文档与普通文章使用相同的编辑和渲染方式。点击表格单元格即可编辑，表头上方图标提供行列操作，悬停可查看说明；大文件按可见内容加载，保存仍使用 Markdown 文本。

![Markdown 编辑流程](markdown-help-overview.svg)

空标题前缀（例如 `#    `）和空无序列表前缀（例如 `- `）保留原文，输入首个正文字符后才排版。列表前缀后输入 `[` 自动补齐 `[]`，光标留在括号中；输入空格或 `x` 后生成任务框，随后可直接输入文字。按 `Ctrl+/` 可在当前分栏切换源码。

使用 `Ctrl+V` 可直接粘贴剪贴板截图。图片保存在文章旁的 `.assets` 文件夹中，未命名文章需要先保存。鼠标拖选可以跨越正文、标题、引用、代码块和表格，拖到窗口上下边缘会继续滚动；选区可复制、剪切、删除、替换，并可撤销。

## 一页速查

### 表格键盘操作

Delete / Backspace 只删除当前格内文字；格尾、格首和空格子不会继续删除表格或相邻正文。删除数据行请使用明确的 Ctrl+Shift+Backspace。

| 操作 | 快捷键 |
| --- | --- |
| 前后单元格；末格续行 | Tab / Shift+Tab |
| 下一行同列；末行续行 | Enter |
| 在当前行下插行 | Ctrl+Enter |
| 当前列左侧 / 右侧插列 | Ctrl+Alt+← / Ctrl+Alt+→ |
| 跳出表格进入正文 | Alt+Enter（兼容 Ctrl+Shift+Enter） |
| 删除当前数据行 | Ctrl+Shift+Backspace |

扩列同时更新整张表；若表格超出当前局部编辑窗口，不会只修改可见行。

**键盘跳出表格：按 `Ctrl+Shift+Enter`，随后直接输入下方正文。** `Ctrl+Enter` 仍在表格内插入新行。源码与渲染模式使用 `Ctrl+/` 切换，光标跟随到相同内容位置。

表格内 Enter 同列下移并在末行续行，Shift+Enter 在当前格内换行；末格 Tab 自动新增行。上下方向键先在单元格可见行中移动，末行向下可到块外。长文字自动换行，多行粘贴保留换行，保存为 `<br />`。编辑时表头上方的图标操作栏和右键菜单提供插删行列、对齐、调整尺寸及整表删除，悬停图标可查看具体操作和快捷键。调整尺寸时直接显示当前数据行数，不包含表头。Ctrl+T 打开插表对话框，新表格的表头为空白，可直接填写。

代码块中 `Ctrl+A` 选中代码正文；`Tab` 插入四个空格或缩进所选行，`Shift+Tab` 退缩进，回车延续缩进并保留代码空行；末行向下进入语言框，再向下进入正文。语言入口仅在选中代码块或悬停时显示。

表格中 `Tab` / `Shift+Tab` 切换单元格，末格按 `Tab` 续行；`Ctrl+Enter` 向下插行，`Ctrl+Shift+Backspace` 删除当前数据行（保留表头和至少一行数据），`Ctrl+A` 选中当前单元格。支持 `Ctrl+Z` / `Ctrl+Y` 撤销和重做。

| 内容 | 输入 | 快捷操作 |
| --- | --- | --- |
| 标题 | `# 标题` | `Ctrl+1`～`Ctrl+6` |
| 粗体 / 斜体 | `**粗体**` / `*斜体*` | `Ctrl+B` / `Ctrl+I` |
| 链接 | `[名称](地址)` | `Ctrl+K` |
| 图片 | `![说明](图片地址)` | `Ctrl+Shift+I` 或拖入图片 |
| 引用 | `> 内容` | 顶部“引用” |
| 列表 | `- 项目` / `1. 步骤` | `Tab` 嵌套，`Shift+Tab` 减少层级 |
| 任务 | `- [ ] 待办` / `- [x] 完成` | 显示真实复选框 |
| 代码块 | 三个反引号 | 选择语言后输入代码 |
| 分割线 | `---` 后回车 | 顶部“分割线” |

## 真实排版效果

### 标题层级

#### 四级标题

##### 五级标题

###### 六级标题

### 行内样式

这是 **粗体**、*斜体*、~~删除线~~、`inline_code()`、[可点击链接](https://www.qt.io) 和 ==高亮文字==。

### 引用

> 引用块拥有连续底色和左侧竖线。
>
> - 引用中也能放列表
> - **粗体** 和 `代码` 会继续渲染
>   * 列表中可以继续缩进子项目
> 1. 引用中也可以使用有序列表
>
>> + 多级引用中同样支持列表

### 嵌套列表

- 一级项目
  - 二级项目：光标位于列表项时按 `Tab`
    - 三级项目
  - 按 `Shift+Tab` 返回上一层
- 回到一级项目

1. 第一步
2. 第二步
   1. 子步骤 A
   2. 子步骤 B

### 任务列表

删除任务框后光标停在当前行；复制任务框会连同隐藏的列表前缀一起复制。插入图片后，光标位于图片后方，可直接接着输入文字。矩形选择不包含渲染中隐藏的空白行，标题与分割线保持渲染；按 `Esc`、普通方向键或普通点击可退出矩形模式。

输入任务状态后会立即补齐源码中的分隔空格，渲染时只显示任务框与正文之间的间距。左右键跨过整个任务框，在框后按退格或在框前按删除可整体移除任务标记。长任务和普通列表换行时与文字对齐，符号留在左侧。

渲染模式中的图片按一个对象移动和删除，点击不会展开图片源码。表格、列表和引用内也可插入图片；需要修改图片地址时可切换源码模式。矩形选择保留列表的渲染外观。

- [x] 已完成：显示选中的任务框
- [ ] 待处理：显示空任务框
- 普通列表：仍显示普通项目符号

### 代码块

```cpp
#include <QString>

QString greeting(const QString &name)
{
    return QStringLiteral("你好，%1！").arg(name);
}
```

### 表格

顶部“表格”按钮可以设置新表格的行列数；把光标放在已有表格内再次点击，可以从末尾增减行列并保留未被裁掉的内容。在渲染模式把鼠标移到单元格边角，会在指针附近出现表格编辑栏，可在当前单元格上方/下方插行、左侧/右侧插列，或删除当前行列。

| 功能 | 操作 | 状态 |
| --- | --- | --- |
| 实时排版 | 直接输入 Markdown | ✅ |
| 嵌套列表 | `Tab` / `Shift+Tab` | ✅ |
| 可调表格 | 顶部“表格”设置行列 | ✅ |

### 分割线

下面就是实际分割线：

---

### 图片

鼠标移到已渲染图片上会显示加号放大镜。单击图片在当前编辑区内放大，预览层显示减号放大镜；再次单击或按 `Esc` 恢复。

下面再次显示内置图片，用于确认图片语法确实生效：

![内置 Markdown 流程图](markdown-help-overview.svg)

## 常用操作

数学公式支持 `$E=mc^2$` 行内写法，或放在一对独立 `$$` 行之间；Mermaid 图表使用语言为 `mermaid` 的代码围栏。点击后编辑源码，离开后显示结果。渲染在本机完成，需要已安装 Edge 或 Chrome；无效语法保留源码。
